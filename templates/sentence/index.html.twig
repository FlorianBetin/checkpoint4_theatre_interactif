{% extends 'base.html.twig' %}

{% block title %}Bon visionnage
{% endblock %}

{% block body %}

	<pre>
        {{ dump(sentence) }}
    </pre>

	<a href="{{ path('app_piece_play', {'piece_slug': piece.slug, 'acte_slug': acte.slug, 'sentence_step': sentence.step - 1}) }}" class="btn btn-primary">Précédente</a>

	<a href="{{ path('app_piece_play', {'piece_slug': piece.slug, 'acte_slug': acte.slug, 'sentence_step': sentence.step + 1}) }}" class="btn btn-primary">Suivant</a>

{% endblock %}
