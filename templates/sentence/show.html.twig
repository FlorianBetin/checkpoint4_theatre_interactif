{% extends 'base.html.twig' %}

{% block title %}Sentence{% endblock %}

{% block body %}
    <h1>Sentence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sentence.id }}</td>
            </tr>
            <tr>
                <th>Background</th>
                <td>{{ sentence.background }}</td>
            </tr>
            <tr>
                <th>Character_one</th>
                <td>{{ sentence.characterOne }}</td>
            </tr>
            <tr>
                <th>Character_two</th>
                <td>{{ sentence.characterTwo }}</td>
            </tr>
            <tr>
                <th>Speaker</th>
                <td>{{ sentence.speaker }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ sentence.content }}</td>
            </tr>
            <tr>
                <th>Step</th>
                <td>{{ sentence.step }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sentence_index') }}">back to list</a>

    <a href="{{ path('app_sentence_edit', {'id': sentence.id}) }}">edit</a>

    {{ include('sentence/_delete_form.html.twig') }}
{% endblock %}
