<form method="post" action="{{ path('app_acte_delete', {'id': acte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acte.id) }}">
    <button class="btn">Delete</button>
</form>
